

/**
 * 此reducer负责全局组件需要在componentDidMount里setState的状态
 */

const initState = {
   
}

export function commRedux(state=initState,action){
  switch(action.type){
    default:
      return state
  }
}
